<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imooc.demo.dao.PictureDao">
    <select id="selectUrlByTagName" resultType="java.lang.String" parameterType="java.lang.String">
        select picture_url
        from picture,pic_tag,tag
        where picture.picture_id=pic_tag.picture_id AND pic_tag.tag_id=tag.tag_id
			AND tag.tag_name=#{tagName}
    </select>
</mapper>
