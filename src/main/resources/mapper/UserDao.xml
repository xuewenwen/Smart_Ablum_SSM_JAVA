<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imooc.demo.dao.UserDao">
    <select id="UserSelectAll" resultType="com.imooc.demo.bo.User">
        select * from user ORDER BY user_id;
    </select>
    <select id="selectUserById" resultType="com.imooc.demo.bo.User">
        select * from user where user_id=#{userId}
    </select>
    <select id="selectUserByName" resultType="com.imooc.demo.bo.User">
        select * from user where user_name=#{userName}
    </select>
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="userId">
        insert into user(user_name,user_password,user_email,user_status,user_size,
		user_register_time,user_last_login_time)
		VALUES
		(#{userName},#{userPassword},#{userEmail},#{userStatus},#{userSize},#{userRegisterTime},#{userLastLoginTime}
		)
    </insert>
    <update id="updateUser" parameterType="com.imooc.demo.bo.User">
        update user
        <set>
            <if test="userName != null">user_name=#{userName},</if>
            <if test="userPassword != null">user_password=#{userPassword},</if>
            <if test="userEmail != null">user_email=#{userEmail}</if>
        </set>
        where user_id=#{userId}
    </update>
    <update id="BanUser" parameterType="com.imooc.demo.bo.User">
		update user
        <set>
            <if test="userStatus != 2">user_status=#{userStatus}</if>
        </set>
        where user_id=#{userId}
	</update>
</mapper>